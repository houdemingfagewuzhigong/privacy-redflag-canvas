name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  static-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check required files
        run: test -f index.html && test -f README.md && test -f README.zh-CN.md
